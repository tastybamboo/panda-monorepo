#!/usr/bin/env bash
# Wrapper script to make 'panda' command available from anywhere in the monorepo

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/../scripts/panda" "$@"
